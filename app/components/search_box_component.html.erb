<%= form_tag @form_path, method: :get, class: "flex justify-center items-center gap-2 max-w-4xl mx-auto w-full px-4" do %>
<div class="flex-1 relative">
    <div class="absolute inset-y-0 left-0 pl-4 flex items-center pointer-events-none">
        <%= phosphor_icon "magnifying-glass", class: "w-5 h-5 text-muted-foreground" %>
    </div>
    <%= text_field_tag @search_param, 
        params[@search_param], 
        class: "w-full py-3 pl-12 pr-4 text-base rounded-lg border border-input bg-white focus:outline-none focus:ring-2 focus:ring-primary",
        placeholder: "Search for albums..." %>
</div>
<%= submit_tag "Search", class: "py-3 px-8 rounded-lg text-base font-medium bg-primary text-primary-foreground hover:bg-primary/90 transition-colors" %>
<% end %>