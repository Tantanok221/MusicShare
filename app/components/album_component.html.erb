<%= link_to album_details_path(@album.id), class: "flex flex-col outline outline-1 outline-border rounded-lg" do %>
<div class="p-6">
    <img src="<%= @album.album_image_url %>" alt="<%= @album.name %>" class="w-full h-64 bg-neutral-300 rounded-lg" />
    <div class="flex flex-col gap-2">
        <h2 class="text-xl font-semibold">
            <%= @album.name %>
        </h2>

        <div class="flex flex-row gap-1.5">
            <div class="flex flex-row gap-4">
                <% @album.genres.each do |genre| %>
                <span class="bg-muted-foreground text-white text-xs px-2 py-1 rounded">
                    <%= genre.genre_name %>
                </span>
                <% end %>
            </div>
        </div>

        <p class="text-xs">The song that changed it all, as obvious as it is necessary! Bill Haley was the man with the
            vision of what the kids wanted to hear and how to transform R&B into digestable form without misinterpreting
            it.</p>

        <div class="flex flex-row justify-between">
            <h2 class="text-xs font-medium">
                <%= @album.artists.map(&:name).join(", ") %> - <%= @album.release_date.year %>
            </h2>
            <div>
                <p class="flex flex-row gap-1 items-center text-xs font-medium">
                    <%= phosphor_icon "star", class: "h-3 w-3" %><%= number_with_precision(@album.rating, precision: 2) %>
                </p>
            </div>
        </div>
    </div>
</div>
<% end %>