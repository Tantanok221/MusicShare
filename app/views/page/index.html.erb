<div>
  <%= render "page/shared/banner" %>
  <div class="flex flex-col gap-4">
    <h3 class="text-black text-2xl font-semibold">
      Recommend Album For You
    </h3>

    <% @albums.each do |album| %>
      <h1> <%= album.name %></h1>
    <% end %>

  </div>
  <%= render "page/shared/list" %>
</div>
